{
  "name": "snpm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Workspace-first Infrastructure HQ Notion automation and project bootstrap tooling.",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "create-project": "node src/cli.mjs create-project",
    "page-diff": "node src/cli.mjs page diff",
    "page-pull": "node src/cli.mjs page pull",
    "page-push": "node src/cli.mjs page push",
    "verify-project": "node src/cli.mjs verify-project",
    "test": "node --test"
  }
}
